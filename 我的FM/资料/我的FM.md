## 页面适配






## 

```js
$.ajax({
  url: url,
  data: data,
  success: success,
  dataType: dataType
});

$(selector).get(url,data,success(response,status,xhr),dataType)
//get() 方法通过远程 HTTP GET 请求载入信息。
```